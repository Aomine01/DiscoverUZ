import Loading from '../loading';

export default function LoadingTestPage() {
    return (
        <div>
            <h1 className="sr-only">Loading Page Demo</h1>
            <Loading />
        </div>
    );
}
